#include "main.h"
/**
 * print_alphabet - main prototype function
 * desc:  prints alphabet letters
 * Return: no Return
 */
void print_alphabet(void)
{

	 int a;

	for (a = 'a' ; a <= 'z' ; a++)
		_putchar(a);
	 _putchar(10);

}
