#include <stdlib.h>
#include <stdio.h>

int main(int argc, char* argv[])
{
printf("argv[2] is %s\n", argv[2]);
printf("argv[0] is %s\n", argv[0]);
printf("argv[argc] is %s\n", argv[argc]);
  
	return (0);
}
