#include <stdio.h>

/**
 *main-entry point of the program
 *
 *Return: Always 0
 */

int main(void)
{
char  *myString = "with proper grammar, but the outcome is a piece of art,";

printf("%s", myString);
return (0);
}
